# 开发顺序与强制停顿点

## 2026-08-14 联合开发授权（覆盖本轮顺序，不覆盖质量门禁）

Ethan 已明确要求阶段 3、4、5 一并完整实现，不再保留占位页，并接入它们依赖的真实登录、会员、支付和账号安全数据。因此，本轮可以并行开发这三个阶段，不再逐阶段等待下一句“开始”。

这项授权只覆盖下面原有的**顺序与中途停顿**，不覆盖任何设计、安全和验收要求：

- 仍须使用冻结 VI 和正式资产，不得临时发挥或用假完成品补洞。
- 订单、会员、资产、身份和安全状态必须来自服务端授权上下文；不得用 URL、客户端缓存或演示数据冒充本人数据。
- 微信身份只能在已有 Member 的显式认证绑定路径中绑定；UnionID 相同也不得自动合并。
- 微信支付结果以验签、解密、幂等通知和主动查单为准，`wx.requestPayment` 的前端成功回调不是入账依据。
- 三阶段完成后统一提交开发者工具真实截图、异常状态、权限隔离和支付回调证据，由 Ethan 验收。未通过前仍不得提交审核或正式发布。

## 阶段 0：只读核对

- 阅读全部 VI、移动设计、会员码主方案和设计图。
- 查看现有 `miniprogram`，列出页面、组件、临时素材和设计偏差。
- 不修改文件。
- 输出首页逐区映射并等待 Ethan。

**停顿点：Ethan 回复“开始首页”。**

## 阶段 1：首页与共享壳层

- 微信动态导航安全区。
- 品牌头部、企业身份、购物车与角标。
- 搜索、福利资产摘要、四个业务入口、员工福利季主视觉。
- 商超到家/生活服务/数码办公/餐饮福利。
- 到店出示会员码。
- 推荐商品和固定五栏底部导航。
- 骨架屏、图片占位、局部失败重试。

**停顿点：提交开发者工具真实截图，与 01 设计板逐项核对；Ethan 确认首页。**

## 阶段 2：分类与商品浏览

- 分类、搜索、商品列表、城市和资格标签。
- 右上角购物车保持一致。
- 商品详情展示来源、履约、库存、配送、售后和资格原因。

**停顿点：Ethan 确认分类和商品详情。**

## 阶段 3：会员码中心

- 动态二维码、条形码、45 秒倒计时。
- 会员身份、认证、企业、资产摘要。
- 正常、未认证、冻结、过期、核验失败状态。
- 数字会员卡、实体/电子券、AI 消费洞察入口。

**停顿点：Ethan 确认会员码四页。**

**本轮状态：已由 2026-08-14 联合开发授权解锁；上述停顿点合并到阶段 3–5 的统一验收，不代表已经完成。**

## 阶段 4：订单与交易

- 购物车、多渠道拆单。
- 结算、福利余额/餐卡/券/积分/微信支付占位。
- 订单列表、详情、物流、售后和退款状态。

**停顿点：Ethan 确认交易闭环。**

**本轮状态：已由 2026-08-14 联合开发授权解锁。订单合规最小闭环同时获批，可提前接入真实订单详情、微信预下单、支付通知、查单兜底和订单中心详情 Path；尚未通过真实支付验收前不得声称外部启用。**

## 阶段 5：我的、账号与认证

- 会员卡、资产、收藏、地址、发票、客服。
- 账号安全、手机未认证、绑定/换绑预留。
- 登录、会员绑定、会话失效和退出登录。

**停顿点：全套视觉和交互验收。**

**本轮状态：已由 2026-08-14 联合开发授权解锁；与阶段 3、4 一并实施和验收。**

## 阶段 6：接口接线

视觉验收通过后才接真实 API。接口接入不得改变已确认的视觉结构；加载、错误、空状态和降级状态必须保留。

**已确认例外（2026-08-14）：**阶段 2 的分类结构从主 Shop 同源机器可读分类契约生成；会员商品目录可以提前经 `commerce-api` 做只读异步接线，以验证同步、空、失败与重试。该例外不允许小程序直连数据库，也不解锁登录、支付、会员码或其他阶段功能。

**新增确认例外（2026-08-14）：**Ethan 后续已明确批准“订单合规最小闭环”，并要求阶段 3、4、5 联合完整实现。这条较新的决议覆盖上一段“不解锁登录、支付、会员码”的限制，但范围只限本轮获批页面及其真实依赖。允许建设：

- 小程序短期 Bearer 会话与微信身份显式绑定；会员授权目标仍为 `storefront`，不另造一套 `miniapp` 权限目标。
- 本人订单详情页，订单中心详情 Path 固定按微信替换的商户订单号打开，服务端重新校验当前 Member / Membership / Tenant / Mall / User。
- 微信支付 JSAPI 预下单、客户端 `wx.requestPayment`、支付通知验签与解密、数据库幂等入账、主动查单和事务出箱。
- 会员码、订单、我的、认证与账号安全页所需的真实服务端数据和异常状态。

仍然禁止：小程序直连数据库、在仓库保存任何微信密钥、用前端回调直接改订单为已支付、自动用 UnionID 合并 Member、在外部条件未配齐时宣称已上线，以及未经 Ethan 另行授权提交审核或正式发布。
