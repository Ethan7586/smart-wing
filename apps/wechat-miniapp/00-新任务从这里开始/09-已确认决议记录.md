# 已确认决议记录

Ethan 逐条确认过的选择。后续任务直接执行，不要重新提问，也不要自行改回。

新增决议时追加，不改写历史行。

---

## 2026-08-14

| #   | 事项             | 决议                                                                                                                                                    | 影响文件                                                                   |
| --- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 1   | 用户界面名称     | 统一「会员码」。`tokens.json` 与 `mobile-platforms.json` 中原有的「翼码」已全部改为「会员码」；内部标识符 `wingCode` / `wing-code` / `member-code` 保留 | `packages/design-system/src/tokens.json`、`mobile-platforms.json`          |
| 2   | 首页扫码入口     | 要。搜索框右侧放 `scan-line`                                                                                                                            | `pages/home/home.wxml`                                                     |
| 3   | 合作卖场         | 要。全部 / 麦德龙 / 沃尔玛 / 山姆 / 大润发 / 永辉，位置在「员工专享福利季」与四大业务分区之间                                                           | `data/demo.js`、`pages/home/home.wxml`                                     |
| 4   | 卖场 Logo        | MVP 用中性文字标签 + `store` 图标。Logo 由 Ethan 提供文件后放入 `miniprogram/assets/partners/`，组件自动切换，布局不动。不从网络抓取 Logo 文件入库      | `pages/home/home.wxml`                                                     |
| 5   | 会员码图标描边   | 调粗到与图标库同重量（24 网格 2px）。源文件 `packages/design-system/src/brand/wing-code-symbol.svg` **不修改**，只在生成小程序资产时换算                | `scripts/build-miniapp-assets.mjs` 的 `CALIBRATE_MEMBER_CODE_STROKE`       |
| 6   | 「生活服务」图标 | `house-plus`（到家服务）。原 `concierge-bell` 弃用                                                                                                      | `data/demo.js`、生成器 MANIFEST                                            |
| 7   | 「AI 洞察」图标  | `bot`（机器人）。**尚未落地** —— AI 入口在会员码页，阶段 3 建设时使用                                                                                   | 待 `pages/membercode/`                                                     |
| 8   | 图标来源         | `lucide-react`（ISC，随仓库 `node_modules`）+ `packages/design-system/src/brand/` 品牌 SVG。同页不混用第三套                                            | `scripts/build-miniapp-assets.mjs`                                         |
| 9   | 底栏标签字号     | 20rpx，行高 28rpx；写入小程序平台映射，由资产管道生成变量，不再在组件中硬编码                                                                           | `mobile-platforms.json`、`styles/tokens.wxss`、`custom-tab-bar/index.wxss` |
| 10  | 阶段 2 数据例外  | 分类结构立即使用主 Shop 同源分类契约；会员商品只经 `commerce-api` 异步读取。不得直连数据库，不得把接口失败伪装成空数据，也不得用演示数据覆盖真实失败    | `catalog-taxonomy.json`、`utils/catalog.js`、`utils/api.js`                |
| 11  | 开发者工具授权   | 页面代码与门禁通过后，无需再次询问，直接打开正确工程、编译、检查 Console、截图；正式 AppID/登录/域名条件齐全时可上传“开发版本”。不含提交审核或正式发布  | `08-如何正确启动和验收.md`、逐页上传报告                                   |

---

## 停顿点确认

| 阶段   | 内容           | 确认                                                        |
| ------ | -------------- | ----------------------------------------------------------- |
| 阶段 1 | 首页与共享壳层 | **Ethan 2026-08-14 回复「首页通过」** —— 阶段 2 解锁        |
| 阶段 2 | 分类与商品浏览 | **Ethan 2026-08-14 回复「开始吧」** —— 已授权开工，尚未验收 |

---

## 尚未裁决

- 底栏中央「会员码」文字与圆盘下沿重叠 18rpx，且比左右四个标签低 4rpx。根因是几何冲突：圆盘 112rpx、上浮 24rpx，在栏内仍占 88rpx，而栏高 108rpx 只剩 20rpx，装不下 28rpx 行高的文字。VI 那句「上浮 24rpx」有两种读法 —— A 圆盘顶部露出栏外 24rpx（当前实现，重叠 18rpx）；B 圆盘中心比普通图标中心上移 24rpx，实际露出 42rpx（重叠仅 4rpx，且不改任何冻结数字）。若要完全分开需露出 56rpx。**Ethan 2026-08-14 决定暂时保持现状**，不阻塞首页验收。
- 企业身份胶囊 60rpx 高，低于 VI 的 88rpx 最小触控。设计稿就是矮胶囊，套上会撑变形。
- 主视觉插画、商品图：无正式素材，当前为标注占位。
- 微信开放平台 UnionID 是否办理 —— 不办则公众号与小程序会产生两个 Member，与冻结决议第 3 条冲突。此项影响数据库设计，越晚决定改动越大。
- 正式小程序 AppID —— 工程仍是 `touristappid`；旧说明写 `wx4df4137881a1d2bc`，Ethan 后台截图显示 `wx4a3a20224e396ff3`。两者冲突，确认前只做开发者工具编译，不上传到错误账号。
