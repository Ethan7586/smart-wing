# 智慧翼企业福利商城——生产型 MVP 开发说明

技术服务方：雍彻科技（YONGCHE TECH）

更新日期：2026-07-24

## 1. 本阶段结论

项目已从“纯前端演示原型”进入“生产型 MVP 底座”建设。现有商城界面继续用于甲方确认交互和视觉；新增的服务端、D1 数据库、权限、订单、内部账户支付、审计等模块，作为后续真实业务的权威数据源。

当前不会把浏览器 `localStorage` 中的余额、订单或库存视为正式数据。正式数据切换必须在企业身份认证和初始化数据确认后进行。

## 2. 已落地的生产能力

### 2.1 四层业务隔离

数据模型按以下四层建立：

1. 平台/集团租户（tenant）
2. 企业（enterprise）
3. 商城（mall）
4. 员工用户（user）

所有商品、库存、账户、订单、支付、售后和审计查询均带租户及商城范围；员工订单额外校验企业和本人用户 ID，避免只靠前端切换造成串库。

### 2.2 核心数据域

已建立 26 张生产业务表：

- 组织与身份：租户、企业、商城、部门、用户
- 权限：角色、权限、角色权限、用户角色
- 商品供应链：供应商、SPU、SKU、库存
- 福利资金：福利/餐卡账户、不可变账户流水
- 交易：购物车、购物车明细、主订单、供应商子订单、订单明细
- 支付售后：支付单、支付分摊、退款、售后
- 治理：审计日志、幂等记录

金额统一以“分”为单位存储，禁止使用浮点数作为资金字段。

### 2.3 数据库级风险控制

- 库存预占超过可用库存时，数据库触发器拒绝整个事务。
- 账户扣减后出现负数时，数据库触发器拒绝整个事务。
- 订单累计实付超过应付金额时，数据库触发器拒绝整个事务。
- 账户流水和审计日志禁止更新、禁止删除。
- 创建订单和支付均要求 `Idempotency-Key`，同一幂等键不能提交不同内容。
- 下单时按供应商自动拆分子订单。
- 收货人姓名、电话和地址使用 AES-256-GCM 加密后入库；未配置密钥时禁止创建订单。

### 2.4 已实现的 API

| 方法 | 地址 | 用途 |
|---|---|---|
| GET | `/api/health` | 数据库、认证和加密配置健康检查 |
| GET | `/api/v1/products` | 按商城、分类分页查询商品和实时可售库存 |
| GET | `/api/v1/bootstrap` | 返回当前员工、权限和商城范围 |
| GET | `/api/v1/accounts` | 查询本人福利卡、餐卡余额 |
| GET | `/api/v1/orders` | 查询本人订单 |
| POST | `/api/v1/orders` | 加密收货信息、预占库存、创建主子订单 |
| POST | `/api/v1/orders/{id}/payments/internal` | 福利卡和餐卡组合支付 |

正式环境的写接口默认拒绝匿名请求。开发身份头仅在 `APP_ENV=development` 且 `AUTH_MODE=development` 两项同时成立时启用。

## 3. 当前刻意未开放的能力

以下事项必须取得甲方的真实资质、账号或业务规则后才能安全接入：

- 企业 SSO、微信小程序登录或手机号认证
- 微信支付/支付宝/银联商户号及回调证书
- 阿里权益及其他供应商商品、库存、订单、发货、退款接口
- 企业组织架构和员工同步数据
- 正式商城、企业、部门和员工初始化数据
- 短信、邮件和企业微信通知通道
- 客服系统、工单 SLA 和人工退款审批规则
- 发票、结算周期、供应商对账及财务科目

在上述信息未到位前，系统采取“安全失败”：允许公开查看测试商品，但不允许匿名下单或扣款。

## 4. 从演示数据切换到生产数据

切换分三步执行：

1. 甲方确认身份方案，完成签名验证与账号绑定。
2. 导入正式租户、企业、商城、员工、商品、SKU、库存和账户期初余额。
3. 将现有页面的数据调用从 `mallService` 切换到 `productionApi`，完成购物车、结算、订单和个人中心的端到端验收。

切换后 `localStorage` 只保留主题、提示关闭状态等设备偏好，不再保存余额、订单、卡券或库存。

## 5. 测试与验收现状

- TypeScript 严格检查通过。
- 11 项自动化测试通过。
- 4 个迁移文件在空数据库中顺序执行成功。
- 库存超卖保护已通过数据库触发验证。
- Vinext/Cloudflare 生产构建通过。
- D1 迁移会随 Sites 发布包一同打包。

## 6. 下一迭代

下一阶段优先顺序：

1. 确认并接入企业身份认证。
2. 建立运营管理后台的商品、库存、订单和员工权限模块。
3. 完成前端正式 API 切换。
4. 接入第一家权益/实物供应商。
5. 接入支付渠道及异步回调、退款。
6. 建立日对账、差错处理、告警、备份和客服工单制度。

