node_modules/
.wrangler/
.next/
dist/
coverage/
.codex-temp/
*.tsbuildinfo
.DS_Store
*.log
.env*
!.env.example

.vercel

output/
tmp/

# Private source documents may contain credentials or customer information.
docs/requirements/private/

# Archived deliveries and parallel worktrees are artifacts, not canonical source.
# They must not participate in search, build, format, test or release (P0A05).
pre-contract-code-merge-20260820/
smart-wing-branch-work/
gitpointer.broken.txt

# Local worker runtime secrets. Never commit.
.dev.vars
**/.dev.vars

# Requirement workbook carries vendor credentials in the interface sheet.
# It stays on disk for reading, never in version control.
docs/福利商城功能清单.xlsx
